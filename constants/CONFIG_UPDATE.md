# 🔧 Konfigurasi API - Update untuk Node.js Backend\n\n## ✅ Perubahan yang Dilakukan:\n\n### 1. **Base URL Updated**\n```typescript\n// Sebelum (PHP)\nBASE_URL: 'http://10.251.109.208/hadirinapp'\n\n// Sesudah (Node.js)\nBASE_URL: 'http://192.168.1.100:3000'\n```\n\n### 2. **Endpoints Updated**\nSemua endpoint sudah dihapus ekstensi `.php`:\n```typescript\n// Sebelum\nLOGIN: '/auth/api/login.php'\nDATA_PEGAWAI: '/admin/pegawai-akun/api/data-pegawai.php'\n\n// Sesudah\nLOGIN: '/auth/api/login'\nDATA_PEGAWAI: '/admin/pegawai-akun/api/data-pegawai'\n```\n\n### 3. **REST API Pattern**\nUpdate method untuk CRUD operations:\n```typescript\n// DELETE pegawai\ndeletePegawai: async (id: number) => {\n  const response = await fetchWithRetry(`${getApiUrl(API_CONFIG.ENDPOINTS.DELETE_PEGAWAI)}/${id}`, {\n    method: 'DELETE'\n  });\n}\n\n// UPDATE pegawai\nupdatePegawai: async (id: number, data: any) => {\n  const response = await fetchWithRetry(`${getApiUrl(API_CONFIG.ENDPOINTS.UPDATE_PEGAWAI)}/${id}`, {\n    method: 'PUT',\n    body: JSON.stringify(data)\n  });\n}\n```\n\n### 4. **Environment Configuration**\nTambah file `constants/env.ts` untuk:\n- Dynamic base URL (development/production)\n- Debug logging\n- Timeout configuration\n\n### 5. **Debug Logging**\nTambah logging untuk development:\n```typescript\ndebugLog('API Request', { url, method });\ndebugLog('API Response', { url, status });\ndebugLog('API Error', { url, error });\n```\n\n## 🚀 Cara Menggunakan:\n\n### Development Mode\n```typescript\n// Otomatis menggunakan localhost jika __DEV__ = true\nBASE_URL: 'http://localhost:3000'\n```\n\n### Production Mode\n```typescript\n// Menggunakan network IP\nBASE_URL: 'http://192.168.1.100:3000'\n```\n\n## 📱 Tidak Ada Perubahan di Component\n\nSemua component React Native **TIDAK PERLU DIUBAH** karena:\n- Response format sama dengan PHP\n- API helper functions tetap sama\n- Error handling tetap sama\n\n## 🔄 Migration Checklist:\n\n✅ **Base URL** - Updated ke Node.js server\n✅ **Endpoints** - Hapus ekstensi .php\n✅ **REST Methods** - Update CRUD operations\n✅ **Environment Config** - Dynamic base URL\n✅ **Debug Logging** - Development debugging\n✅ **Error Handling** - Tetap sama\n✅ **Response Format** - Tetap sama\n\n## 🎯 Status: READY!\n\nKonfigurasi sudah siap untuk Node.js backend. Mobile app akan otomatis menggunakan endpoint yang benar.